Arduino安裝下列元件：

TM1637七段顯示器，CLK(D2)、DIO(D3)。

MAX7219控制8X8LED陣列，DATA(D12), CLK(D11), CS(D10)。

6顆WS2812LED，DIN(D6)。

開關SW1(D4),SW2(D5)。

可變電阻(A0)。

紅色LED(A1),黃色LED(A2),綠色LED(A3)。

三色LED,R(D9),G(D8),B(D7)。
